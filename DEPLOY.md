# دليل النشر — خيارات مجانية 100% (بدون بطاقة بنكية)

## ✅ الخيار 1 — مفعّل الآن (الأسهل، صفر حسابات)
**الرابط الثابت الحالي:**
`https://ee4c0cd3c590be6e-47-237-237-238.serveousercontent.com`

- خدمة serveo (مجانية، بلا حساب وبلا بطاقة)
- الرابط **ثابت** (مشتق من مفتاح ssh على هذا الجهاز) — يعود للظهور تلقائيًا بعد أي إعادة تشغيل للخادم طالما الجهاز يعمل
- **الحد**: يستمر طالما هذا الجهاز (السيرفر) شغّال — إذا توقف الجهاز نهائيًا يتوقف الموقع

## 🥇 الخيار 2 — الأفضل طويل المدى: نفق Cloudflare مسمّى (مجاني، بلا بطاقة)
يلزم فقط **حساب Cloudflare مجاني** (تسجيل بالبريد فقط — لا بطاقة).

```bash
# على هذا الجهاز (سأنفذها معك خطوة بخطوة):
cloudflared tunnel login                      # يفتح متصفحًا لتسجيل الدخول
cloudflared tunnel create meloverse           # ينشئ النفق المسمّى
cloudflared tunnel route dns meloverse meloverse.trycloudflare.com
cloudflared tunnel run meloverse              # رابط ثابت: meloverse.trycloudflare.com
```
- الميزة: **رابط ثابت يخصك** + يعمل حتى لو تغيّر IP الجهاز

## 🥈 الخيار 3 — استضافة سحابية دائمة: Render + GitHub (مجاني، بلا بطاقة)
1. أنشئ حساب GitHub مجاني (بريد فقط)
2. ارفع هذا المشروع (سأجهّز لك الأمر الجاهز) — ملف `render.yaml` موجود بالفعل في الجذر
3. في Render: New → Blueprint → استورد مستودع GitHub → ينشر تلقائيًا
4. النتيجة: **رابط دائم مجاني** (مثل `meloverse.onrender.com`) يعمل 24/7 حتى لو جهازك مغلق + قرص بيانات محفوظ

## 🥉 بدائل أخرى (مجانية، بلا بطاقة)
- **Glitch** (glitch.com): استضافة Node مجانية بدون بطاقة — رفع المشروع (web/) مباشرة
- **Replit** (replit.com): مجاني مع نوم تلقائي
- ملاحظة: خيارات "المتجر" (Vercel/Netlify) تحتاج تحويلًا معماريًا لأن خادمنا يشغّل مهام خلفية مستمرة

## 🔄 ماذا يحدث للـ APK مع كل خيار؟
- التطبيق يتصل بـ `SERVER_URL` (مضبوط حاليًا على الرابط الثابت serveo)
- عند تغيير الاستضافة، نعدّل سطرًا واحدًا في `android/app/build.gradle.kts` ونعيد البناء (دقيقة واحدة)
- **فاحص التحديثات** في التطبيق يقرأ `/api/update/latest` من السيرفر تلقائيًا

## 📌 ملخص التوصية
- **أسرع حل الآن**: serveo (مفعّل) ✓
- **الأفضل بلا جهاز**: Render (حساب GitHub + Render، كلاهما مجاني بلا بطاقة) — المشروع جاهز
- **الأفضل برابط يخصك**: نفق Cloudflare المسمّى (حساب مجاني)

## ⚠️ توضيح مهم بعد التجربة (خدمة serveo)
جرّبت serveo فعليًا: **الرابط ليس ثابتًا** — يتغير عند كل إعادة اتصال رغم استخدام المفتاح نفسه. لذلك:
- **الرابط الحالي المعتمد**: نفق trycloudflare (يعمل الآن ولا نعيد تشغيله — يبقى ثابتًا حتى إعادة تشغيل الخادم)
- **الحلول الدائمة الحقيقية** (مجانية، بلا بطاقة) تتطلب حسابًا مجانيًا واحدًا فقط منك:
  1. **Cloudflare المسمّى** (الأفضل): حساب مجاني بالبريد فقط → رابط ثابت يخصك مثل `meloverse.trycloudflare.com` — خطوات في الأعلى، وأنفّذها معك مباشرة
  2. **Render + GitHub**: حسابان مجانيان → رابط دائم يعمل 24/7 حتى لو أُطفئ جهازك
